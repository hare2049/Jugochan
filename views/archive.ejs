<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/archive.css' />
  </head>
  <body>
    archive
  <div id="threads">
    <% for (let i = 0; i < threads.length; i++) { %>
    <div id="thread<%=threads[i].id%>" class="thread">
      <span><%=threads[i].thread_topic%> ' ' <%=threads[i].text%></span>
    </div>
    <%}%>
  </div>

  <a id="delete">Delete</a>


  </body>
</html>
